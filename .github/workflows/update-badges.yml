name: Update Credly Badges

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Generate the Credly badge section
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main
        with:
          CREDLY_USER: rhmtazad

      # Replace Credly's low-resolution 80x80 images
      # with the original image URLs and control the display size.
      - name: Improve Badge Quality
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          path = Path("README.md")
          text = path.read_text(encoding="utf-8")

          start_marker = "<!--START_SECTION:badges-->"
          end_marker = "<!--END_SECTION:badges-->"

          if start_marker not in text or end_marker not in text:
              raise SystemExit("Credly badge section not found.")

          before, rest = text.split(start_marker, 1)
          badges, after = rest.split(end_marker, 1)

          # Remove Credly's 80x80 thumbnail path.
          badges = badges.replace(
              "https://images.credly.com/size/80x80/",
              "https://images.credly.com/"
          )

          # Increase the displayed badge size.
          badges = re.sub(
              r'width="80"',
              'width="100"',
              badges
          )

          badges = re.sub(
              r'height="80"',
              'height="100"',
              badges
          )

          path.write_text(
              before
              + start_marker
              + badges
              + end_marker
              + after,
              encoding="utf-8"
          )
          PY

      # Commit only if README actually changed
      - name: Commit Changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add README.md

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Update Credly badges"
            git push
          fi
